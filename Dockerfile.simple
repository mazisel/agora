# Simple runtime build approach
FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source code
COPY . .

# Expose port
EXPOSE 3001

# Build and start at runtime
CMD ["sh", "-c", "npm run build && npm start"]